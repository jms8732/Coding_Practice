package combination;


public class test2 {
	public static void main(String[] args) {
		int [] array= {1,2,3};
		int [] value = new int[2];
		
		combination(0,value,array,0);
	}
	
	private static void combination(int depth, int [] value, int [] array,int next) {
		if(value.length == next) {
			print(value);
			return;
		}
		
		for(int i = next ; i < array.length ; i++) {
			value[depth] = array[i];
			dfs(depth+1,value,array,n,i+1);
		}
	}
	
	private static void print(int [] value)
	{
		for(int i : value)
			System.out.print(i + " " );
		System.out.println();
	}
}
