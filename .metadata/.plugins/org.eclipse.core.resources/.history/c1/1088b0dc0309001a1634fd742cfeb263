package programmers;

//[1차 캐시]

import java.util.*;

public class problem_42 {
	public static void main(String[] args) {
		String [] tmp = {"Jeju", "Pangyo", "NewYork", "LA","SanFrancisco" ,"Seoul","Rome", "Paris", "Jeju", "NewYork", "Rome"};
		int result = solution(5,tmp);
		System.out.println(result);
	}

	public static int solution(int cacheSize, String[] cities) {
		List<String> list = new ArrayList<>();
		int time =0 ;
		
		if(cacheSize ==0)
			return cities.length * 5;
		
		for(int i =0 ; i< cities.length; i++) {
			if(list.size() < cacheSize) { //캐시가 비었을 경우
				int j =0 ;
				for(j = 0 ; j< cacheSize ; j++) {
					list.add(0, cities[j]); //앞에 먼저 채워넣는다.
					time += 5; //없을 경우 cache miss
				}
				i = --j;
			}else {
				//캐시가 다 차있는 경우
				boolean check = false;
				int idx =0 ;
				for(int j =0 ; j< list.size(); j++) {
					idx =j ;
					if(list.get(j).equals(cities[i])) {
						check = true;
						break;
					}
				}
				String tmp = null;
				if(check) {
					//값이 있는 경우
					tmp = list.get(idx);
					time += 1;
				}
				else {
					//값이 없는 경우
					tmp = cities[i];
					time += 5;
				}
				list.remove(idx);
				list.add(0,tmp);
			}
		}
		return time;
	}
}
