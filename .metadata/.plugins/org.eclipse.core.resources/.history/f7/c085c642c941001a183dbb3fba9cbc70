package samsung_Atype;

//야구공
import java.util.*;
import java.io.*;

public class problem_17281 {
	static int inning;
	static BufferedReader br;
	static int answer;
	public static void main(String[] args) throws IOException{
		br =new BufferedReader(new InputStreamReader(System.in));
		
		inning = Integer.parseInt(br.readLine());
		int [] value = new int[9];
		
		for(int i =0 ; i < 9 ; i++) value[i] = i+1;
		int depth =0 ;
		dfs(depth,value);
	}
	
	private static void dfs(int depth, int [] value) throws IOException{
		if(depth == value.length) {
			int tmpValue = 0;
			for(int i =0 ; i <inning ; i++) {
				//각 이닝에 대한 플레이어들의 기록
				Queue<Integer> players = new LinkedList<>();
				StringTokenizer st = new StringTokenizer(br.readLine());
				
				for(int j =0 ; j < 9 ; j++) players.add(Integer.parseInt(st.nextToken()));
				
				tmpValue += startGame(players);
				
			}
			
			return;
		}
		
		for(int i = depth; i < value.length ; i++) {
			swap(depth,i,value);
			dfs(depth+1,value);
			swap(i,depth,value);
		}
	}
	
	private static int startGame(Queue<Integer> players) {
		int answer =0 ;
		
		
		return answer;
	}

	
	private static void swap(int depth , int i, int []array) {
		int tmp = array[depth];
		array[depth] = array[i];
		array[i] = tmp;
	}
	
	private static void print(int[] array) {
		for(int i =0 ; i< array.length ; i++)
			System.out.print(array[i] + " " );
		System.out.println();
	}
	
	private static class Node{
		int number, role;
		public Node(int n ,int r) {
			this.number = n;
			this.role =r;
		}
	}
}
